- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.config/astronvim/lua/user:
      create: true
      path: astronvim
    # ~/.tmux.conf: tmux.conf
    # ~/.vim: vim
    # ~/.vimrc: vimrc

# - create:
# - ~/downloads
# - ~/.vim/undo-history

- shell:
    - [git submodule update --init --recursive, Installing submodules]
